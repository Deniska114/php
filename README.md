# Coffee Lover - Динамічний PHP сайт з роутером

Проєкт динамічного PHP сайту з використанням Composer та сучасних PHP бібліотек.

## 📦 Встановлені пакети через Composer

- **monolog/monolog** (^3.0) - Потужна система логування
- **symfony/var-dumper** (^6.0) - Інструменти для дебагу та красивого виведення змінних
- **guzzlehttp/guzzle** (^7.0) - HTTP клієнт для роботи з API

## 🚀 Встановлення

1. Встановіть пакети по одному через Composer (як вимагає завдання):
```bash
composer require monolog/monolog
composer require symfony/var-dumper
composer require guzzlehttp/guzzle
```

Або встановіть всі залежності одразу:
```bash
composer install
```

## 📁 Структура проєкту

```
sait/
├── index.php              # Головний роутер
├── composer.json          # Конфігурація Composer
├── composer_example.php   # Приклади використання пакетів
├── users.php              # Приклад ієрархії класів User
├── pages/                 # Сторінки сайту
│   ├── home.php
│   ├── login.php
│   └── 404.php
├── index.html             # HTML головної сторінки
├── login.html             # HTML сторінки логіну
├── style.css              # Стилі сайту
└── logs/                  # Папка для логів (створюється автоматично)
```

## 🎯 Використання

### Запуск локального сервера:
```bash
php -S localhost:8000
```

### Перегляд прикладів використання пакетів:
Відкрийте в браузері: `http://localhost:8000/composer_example.php`

### Доступні сторінки:
- Головна: `http://localhost:8000/?page=home`
- Логін: `http://localhost:8000/?page=login`
- 404: `http://localhost:8000/?page=неіснуюча_сторінка`

## 🔧 Git налаштування

Для створення окремої гілки з Composer проєктом:

```bash
# Ініціалізуйте репозиторій (якщо ще не зроблено)
git init

# Створіть окрему гілку для Composer проєкту
git checkout -b composer-project

# Додайте файли
git add .
git commit -m "Додано Composer проєкт з пакетами monolog, var-dumper та guzzle"

# Якщо є віддалений репозиторій
git remote add origin <URL_вашого_репозиторію>
git push -u origin composer-project
```

## 📚 Приклади використання пакетів

### Monolog (Логування)
```php
use Monolog\Logger;
use Monolog\Handler\StreamHandler;

$logger = new Logger('app');
$logger->pushHandler(new StreamHandler('logs/app.log', Logger::DEBUG));
$logger->info('Повідомлення');
```

### Symfony VarDumper (Дебаг)
```php
use Symfony\Component\VarDumper\VarDumper;

dump($variable); // Красиве виведення змінної
```

### Guzzle HTTP (HTTP запити)
```php
use GuzzleHttp\Client;

$client = new Client();
$response = $client->get('https://api.example.com/data');
$data = json_decode($response->getBody()->getContents(), true);
```

Детальні приклади дивіться в файлі `composer_example.php`.

## 👤 Автор

Deniska-meow

## 📄 Ліцензія

Проєкт створено для навчальних цілей.

